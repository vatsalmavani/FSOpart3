{
  "name": "fsopart3",
  "version": "1.0.0",
  "description": "exercises from full stack open part 3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf ./dist && cd ../FSO/part02/phonebook && npm run build && cp -r ./dist ../../../FSOpart3/",
    "build:full": "npm run build:ui && git add . && git commit -m updateUI"
  },
  "author": "vatsal",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
